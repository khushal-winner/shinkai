import React from "react";

const MainLayout = ({ children }) => {
  //  redirect to onboarding page
  return <div className="w-full h-full px-4 mt-10">{children}</div>;
};

export default MainLayout;
